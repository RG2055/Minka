# Radio Record metadata proxy used by js/radio.js and the Lācītis player.
# (Kept its original dashboard name; the code used to live only there.)
name = "ancient-bush-28d0"
main = "src/index.js"
compatibility_date = "2026-02-17"
account_id = "a678c2143bd64a6ef4b783aac9953e94"


# Own address on rgapp.page; the workers.dev address stays on for apps
# installed before the move.
workers_dev = true
routes = [{ pattern = "radio.rgapp.page", custom_domain = true }]
[observability]
enabled = true
